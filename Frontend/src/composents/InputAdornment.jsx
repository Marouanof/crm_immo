const InputAdornment = {
  defaultProps: {},
  styleOverrides: {
    root: ({ theme }) => ({
      color: theme.palette.text.secondary,
      margin: 0,
    }),
  },
};

export default InputAdornment;
