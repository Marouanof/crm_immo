{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks",
    "buildCommand": "pip install -r Backend/crmB/requirements.txt"
  },
  "deploy": {
    "startCommand": "gunicorn Backend.crmB.crmB.wsgi:application"
  }
}